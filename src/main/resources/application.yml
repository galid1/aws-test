secret:
  data: local

spring:
  h2:
    console:
      enabled: true

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: sa

logging:
  level:
    org:
      springframework:
        cache: TRACE